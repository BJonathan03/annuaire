{% extends 'base.html.twig' %}


{% block body %}
    <div class="main">
    <div class="main-inner">
    <div class="container">
    <div class="content">



    <div class="page-title">
        <h1>Bienvenue dans la liste des services proposés par notre site</h1>
    </div><!-- /.page-title -->
    <div class="posts post-detail">

        {% for service in services %}

            <h2>{{ service.name }}</h2>
            <a href="{{ path('service_show', {'id':service.id}) }}" class="btn btn-primary">Plus d'infos...</a>
        {% endfor %}

    </div><!-- /.comments -->

    <hr/>

{% endblock %}