{% for vendor in vendors %}
    {% if  vendor.banni == 0 %}
        <li>
            <a href="{{ path('vendor_show', {'id':vendor.id }) }}">{{ vendor.name }}</a>
        </li>
    {% endif %}
{% endfor %}