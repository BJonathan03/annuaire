{% extends 'base.html.twig' %}


{% block body %}
    <div class="main">
    <div class="main-inner">
    <div class="container">
    <div class="content">



    <div class="page-title">
        <h1>Bienvenue dans la liste prestataires de services</h1>
    </div><!-- /.page-title -->
    <div class="posts post-detail">

        {% for vendor in vendors %}

            <h2>{{ vendor.name }} </h2>
            <a href="{{ path('vendor_show', {'id':vendor.id}) }}" class="btn btn-primary">Plus d'infos...</a>
        {% endfor %}

    </div><!-- /.comments -->

    <hr/>

    </div>
    </div>
    </div>
    </div>

{% endblock %}